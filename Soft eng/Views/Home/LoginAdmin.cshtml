@{
    ViewBag.Title = "Login (Admin)";
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Login - Saint Isidore Academy</title>
    <link rel="stylesheet" href="~/css/login.admin.css" />
</head>
<body>

    <div class="login-card">
        <div class="logo-container">
            <img src="https://cdn-icons-png.flaticon.com/512/1946/1946429.png" alt="Admin Logo" />
            <h3 style="color:white;margin-top:12px; text-align:center;">
                SAINT ISIDORE ACADEMY - ADMIN
            </h3>
        </div>

        <form id="loginForm" asp-action="Login" asp-controller="Home" method="post">

            <!-- ?? ERROR MESSAGE -->
            @if (ViewBag.ErrorMessage != null)
            {
                <div class="login-error">
                    @ViewBag.ErrorMessage
                </div>
            }

            <div class="form-group">
                <label>Email</label>
                <input type="text"
                       name="email"
                       class="@(ViewBag.ErrorMessage != null ? "input-error" : "")"
                       required />
            </div>

            <div class="form-group">
                <label>Password</label>
                <div class="password-wrap" style="display:flex;align-items:center;gap:8px;">
                    <input type="password"
                           name="password"
                           class="@(ViewBag.ErrorMessage != null ? "input-error" : "")"
                           required />

                    <button type="button" class="toggle-password" data-target="password"
                            aria-label="Toggle password visibility"
                            style="background:none;border:none;padding:4px;">
                        <svg class="icon-eye" xmlns="http://www.w3.org/2000/svg" width="20" height="20"
                             viewBox="0 0 24 24" fill="none" stroke="currentColor"
                             stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8S1 12 1 12z"></path>
                            <circle cx="12" cy="12" r="3"></circle>
                        </svg>

                        <svg class="icon-eye-off" xmlns="http://www.w3.org/2000/svg" width="20" height="20"
                             viewBox="0 0 24 24" fill="none" stroke="currentColor"
                             stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                             style="display:none">
                            <path d="M17.94 17.94A10.94 10.94 0 0 1 12 20c-7 0-11-8-11-8a21.42 21.42 0 0 1 5-5.94"></path>
                            <path d="M1 1l22 22"></path>
                        </svg>
                    </button>
                </div>

                <a href="@Url.Action("ForgotPasswordAdmin", "Home")" class="forgot-password">
                    Forgot Password?
                </a>
            </div>

            <button type="submit" class="btn-login">Log In</button>

            <div class="footer-text">
                <span>Don't have an account?</span>
                <a href="@Url.Action("RegisterAdmin", "Home")">Create Account</a>
            </div>

        </form>
    </div>

    <script src="~/js/login.admin.js"></script>
    <script src="~/js/passwordToggle.admin.js"></script>
</body>
</html>
